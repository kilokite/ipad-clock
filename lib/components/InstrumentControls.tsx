'use client'

// Compatibility entry point. New code can import individual controls from
// `./instrument-controls` while existing consumers keep the original path.
export * from './instrument-controls'
